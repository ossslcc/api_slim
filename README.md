# Welcome to ApiRest for Acredita2.App
REST API builded with slim framework.

## Install
to install it you need these simple instructions.
```
git clone [URL_GIT_REPOSITORY] [DIRECTORY]
cd [DIRECTORY]
php composer.phar install
```
